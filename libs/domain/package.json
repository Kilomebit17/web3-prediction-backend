{
  "name": "@pred/domain",
  "version": "0.0.1",
  "description": "Pure business logic — NO NestJS, Prisma, Redis, or infrastructure imports allowed",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "jest --config jest.config.ts",
    "test:cov": "jest --config jest.config.ts --coverage"
  },
  "dependencies": {
    "@pred/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.9.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.7.2"
  }
}
